# 论文 v0→v1 修订记录

## 已完成

- 根据 CQ-08 随附说明、`RDMapParser.m` 和原始目录核实并重写“数据集与预处理”：补充 1 550 份原始样本组成、标签合并与固定划分中未纳入的 1008 号航迹，说明 I/Q 原始回波经 Taylor 加窗、FFT、频谱搬移、dB 化和实际速度轴计算生成 RD，并补充 31×900 统一化、dB 域插值、速度掩码、训练集归一化统计量及点迹-航迹时间匹配过程。
- 将论文正文和表格中的无人机、飞鸟、空飘球、杂波、未知目标统一使用中文名称；代码标签、模型分支名和参考文献原题名保持原样。
- 重写引言收束段：以航迹连续运动证据与 RD 瞬时距离-速度结构的互补及粒度不一致为问题起点，采用“问题—为此—方法—验证”的因果链明确本文双域融合、质量感知门控和 Bird/Unknown 诊断三项创新工作。
- 按顺序编码制重排全部参考文献：引言中的首次引用顺序为 `[1]` 至 `[15]`，正文引文和参考文献表同步更新，不再沿用文献收集阶段的临时编号。
- 题目及结论中的任务概括统一改为“低空目标识别”；五分类仍作为数据集与实验评价的具体任务设置保留在正文中。
- 移除稿件中将本文数据与方法误限定为“外辐射源雷达”的表述：标题、引言和结论统一改为面向低慢小目标的 TR--RD 双域五分类；参考文献[10]仅作为航迹识别相关工作保留，不再被写成本文数据来源。
- 调整引言引用方式：取消将多篇文献集中堆叠在单一论断后的写法；按“机场低空安全、航迹识别、微动机理、回波表征、RD 分类”分别放置最直接的 1--2 篇依据，使引用随论述推进而出现。
- 再次压缩并重构引言：由 7 段、约 1986 个非空白字符调整为 4 段，按“低空监视需求—航迹与回波线索—航迹级融合缺口—本文方案”的逻辑推进；删除对“低慢小”、五类标签及 RD/微多普勒关系的专门定义式说明，将必要边界自然融入相关论述，避免引言写成概念说明书。
- 重写引言：加入无人机规模增长、机场净空与鸟击风险等现实应用背景；按“现实问题→航迹证据→回波证据→双域融合缺口”的逻辑重新组织文献，不再泛泛罗列网络名称。
- 根据时效性要求，进一步将引言首段改为无引文的现实背景描述，删除具体年份和注册量统计，避免背景数据过时；具体技术判断和研究依据保留在后续文献段落中。
- 按“现实需求→两类观测证据→航迹域进展与边界→回波域进展与边界→融合动机与检验条件→本文方案与贡献”的因果链整体重写引言；文献改为论证环节的自然支撑，不再使用“文献[X]指出”句式，并补充本文三类贡献的铺垫。
- 增加固定 F split 的三分区类别计数表。
- 明确 `Other` 与 `Unknown` 的标签对应关系。
- 给出 rescue、harm 和净变化的操作性定义。
- 增加一组固定配对测试集诊断结果，并绑定 artifact、checkpoint、epoch、RD 预处理和门控来源。
- 明确验证集校准门控与严格训练集 OOF 门控的差异。
- 将“融合一定提升”的表述改为基于错误互补性和校准协议的条件性结论。
- 在讨论和结论中加入当前门控未超过 RD-only 的证据边界。
- 将“低慢小”重写为工程观测概念：其共同困难来自低高度、低速度、小尺度或低 RCS/弱回波，而非由单一物理阈值或固定实体类别定义；明确微多普勒/微动、电磁散射和宏观运动是三类互补分析信息。
- 明确五分类标签边界：Drone、Bird、Balloon 为物理目标类别；Clutter 和 Unknown 分别为杂波响应与证据不足状态，本文任务应表述为“低慢小目标及其易混淆背景/未知状态的五分类识别”。
- 明确 RD 图与微多普勒图并非同义：前者是距离—径向速度平面表征，后者是微动导致的附加多普勒调制及其时频表现；本文不再将 RD 图直接等同于微多普勒特征。
- 将外部参考文献库切换为 `C:\Users\Surfa\Desktop\可用`，重建为 15 条文献的引用表；移除旧稿中不在该目录的条目，并在引言中完成 `[1]`–`[15]` 的主题化引用。
- 为本地全文缺少完整出版元数据的条目增加“待核”标记，禁止以推测补全 DOI、卷期和页码。
- 新增 R2-900 固定验证集五类代表性 RD 图及可审计样本清单：每类按正确类别概率中位数规则选取航迹和帧，图像使用统一速度坐标与 0～100 dB 裁剪后的原始 RD 幅度，不将单样例视觉差异作为统计性判别结论。
- 在五类 RD 图后补充图像解释：从能量集中程度、速度响应宽度和距离向延展方式进行定性说明，并明确 RD 图与微多普勒瀑布图的坐标含义差异，避免把单帧图像直接解释为微动机理。
- 将五类 RD 图说明改为定量归因：新增 R2-900 全部验证航迹的速度、距离、局部对比和跨帧稳定性统计，并对飞鸟→未知目标、未知目标→飞鸟两种形态偏移给出分别解释；不再使用“思路相近”作为图像分析结论。
- 将图 1 的样例选择由“类别预测概率中位数”更新为“多维 RD 结构中位数”：以近零能量、速度宽度、局部对比、距离集中度和帧间稳定性共同确定类别代表航迹，再选择该航迹的中位结构帧；图中概率标签明确改为航迹级类别预测概率。
- 将图 1 进一步替换为五类类别聚合 RD 结构图：每条航迹先做帧均值并去除逐距离单元速度向背景，再按真实类别取航迹中位数；增加近零速度局部图、同尺度颜色条及速度剖面半峰宽度，避免单帧噪声和动态范围掩盖类别差异。
- 在图 1 前补充“窄/宽速度响应”的物理含义：窄表示能量集中在相近径向速度区间，宽表示回波包含多个相近速度成分，并将部件运动、姿态变化和回波起伏作为响应展宽的可能来源；同时说明该宽度不等同于目标整体速度大小。
- 按中文核心论文行文重新组织图 1 至表 1 的说明，改为“概念界定—类别聚合—指标定义—逐类观察—双向错分解释”的连续结构，压缩模板化总结句和重复的“本文”表述。
- 重新核验固定验证集 232 条航迹的 15 项 RD 结构量，采用非参数检验、效应量、多重校正、冗余检查及重复分层交叉验证筛选正文特征；表 1 改用中位数[IQR]，主线仅保留速度响应宽度、距离峰值能量占比和相邻帧速度剖面相似度。
- 删除由类别聚合图直接外推的附加峰、翼部、背景方向和固定距离行等描述；明确无人机的宽速度响应与距离向集中、空飘球较低的跨帧重复性是当前证据最充分的总体差异，其余类别需依靠多项结构联合判别。
- 将飞鸟/未知目标错例从预处理章节移至“RD 结构统计与模型敏感性分析”，区分全体类别分布、人工结构量和模型归因；明确全体飞鸟/未知目标的响应宽度不具区分力，4 条与 2 条直接错例只用于描述当前模型边界。
- 重写空飘球跨帧相似度的解释，补充相似度的计算含义、当前验证集中的数值差异及其作为辅助区分特征的实际作用，避免直接用“重复性较弱”等抽象表述代替解释。
- 将 RD 结构解释段中的具体统计数值集中保留在表格，正文改为“指标含义—类别现象—分类作用”的叙述顺序；同步压缩飞鸟/未知目标归因与错分段，只描述当前模型的边界现象。
- 依据实际特征提取脚本补充 RD 结构量定义：明确速度响应宽度由距离维最大响应形成的速度剖面计算，距离峰值能量占比由归一化距离功率剖面峰值计算，相邻帧相似度采用相邻速度剖面的余弦相似度；新增三项主结构量的互补作用、空飘球跨帧变化的适用边界及飞鸟/未知目标归因结果的证据层级说明，解释段不再重复表格中的具体统计数值。
- 按“先写可分辨结构、再交代粗分类范围”的方式收束 RD 特征解释：突出无人机的宽速度响应与距离能量集中、空飘球的帧间结构变化，以及飞鸟/未知目标在中心速度形态和局部速度对比上的模型敏感证据；删除以“不能区分”为主体的重复表述。
- 参照航迹运动特征论文的“原始航迹量—宏观统计量—运动含义”组织方式扩充 1.4：将 TR 输入分为逐点 $[T,15]$ 序列和完整航迹上的 22 维统计向量，按速度与加减速、航向与轨迹形态、径向运动与回波、高度与数据质量四组说明计算逻辑，并注明当前代码中速度差、航向差和离散曲折度的实际定义，避免将统计代理量写成未经实现的连续时间物理量。
- 将 1.4 中三向速度统一记为 $v_x,v_y,v_z$，删除东/北/天方向的展开式说明；按参考论文的分条公式结构补充速度统计、速度变化、悬停比例、环绕航向差、轨迹曲折度、转向敏捷性、振荡因子、径向速度平坦度及缺失标志的定义，确保文字与 `radar_fusion/track_features.py` 的实际实现一致。
- 重构 1.4 的航迹特征表述：逐点输入统一改为不含默认缺失高度的 $[T,14]$，以表格完整列出 14 个逐点量和 22 个航迹统计量；正文仅对速度与速度变化、航向与轨迹形态、径向运动和回波质量四组代表性特征作解释，高度相关统计槽位明确仅为缺失占位，不作为物理判别依据。
- 依据 B01 固定 F 验证集的置换归因结果重构 1.4：新增“归因结果与运动判别线索—点迹/航迹匹配与逐点序列—航迹统计特征提取”三级结构，补充逐点序列和航迹统计特征图；以信噪比、总速度、方位角、径向/水平速度及平均航向变化量作为优先解释线索，未将高度占位或无明显性能下降的统计量写成类别判别依据。
- 新增 B01 航迹归因产物：验证集基线 Macro-F1=0.8695 与权威结果复现一致；生成 `artifacts/tr_feature_attribution_b01_fsplit_val/tr_feature_importance.png`、`tr_trajectory_feature_examples.png`、`permutation_importance.csv` 和中文报告 `docs/tr_feature_attribution_b01_report.md`。典型航迹样本按类内代表性与类间结构距离联合规则选择，并保存航迹编号及结构向量。

## 尚未完成

- 严格 OOF 门控的最终测试主表。
- [1]、[2]、[4]、[5]、[12] 的正式元数据核验。
- 模型结构图、数据流程图、错误互补图和 Bird/Unknown 样本可视化。
- 在最终结果锁定后同步更新中英文摘要。
